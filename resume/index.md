Read [[Work Experience]] when modifying resume.tex for job description

To avoid clutter from LaTeX build files, compile with: `latexmk -pdf -outdir=build resume.tex` (this puts `.aux/.log/.fls/...` in `build/` instead of this folder).

